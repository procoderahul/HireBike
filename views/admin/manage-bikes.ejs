<head>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Other head elements like meta tags, title, etc. -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
</head>
<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/admin/admin-home">HireBike</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#adminNavbar" aria-controls="adminNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="adminNavbar">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/admin/add-bike">Add Bike</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/admin/manage-bikes">Manage Bikes</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/admin/manage-bookings">Manage Bookings</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/admin/manage-users">View Users</a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link btn btn-danger text-white" href="/logout">Logout</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="container my-5">
  <h2>Manage Bikes</h2>
  <table class="table table-striped table-hover">
    <thead class="table-dark">
      <tr>
        <th>Model</th>
        <th>Price Per Day</th>
        <th>Image</th>
        <th>Available</th> <!-- New column -->
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% bikes.forEach(bike => { %>
        <tr>
          <td><%= bike.model %></td>
          <td>₹<%= bike.pricePerDay %></td>
          <td><img src="/uploads/<%= bike.image %>" alt="<%= bike.model %>" width="100"></td>
          <td>
            <form action="/admin/toggle-availability/<%= bike._id %>" method="POST">
              <button type="submit" class="btn <%= bike.available ? 'btn-success' : 'btn-danger' %>">
                <%= bike.available ? 'Yes' : 'No' %>
              </button>
            </form>
          </td>
          <td>
            <a href="/admin/edit-bike/<%= bike._id %>" class="btn btn-warning">Edit</a>
            
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<%- include('../partials/footer') %>